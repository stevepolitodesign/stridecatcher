<tr>
    <th scope="col"><%= total.starting_on.strftime("%D") %> through <%= total.starting_on.end_of_week.strftime("%D") %></th>
    <th scope="col"><%= total.distance_in_miles %></th>
    <th scope="col"><%= format_duration(total.duration) %></th>
</tr>